#Create a Sample Python Web Application:
#Create a simple Flask app (app.py).

#Install Flask:

pip install flask

#Create app.py:

from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello, Kubernetes! This is a containerized app."

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)

#Create requirements.txt

flask


